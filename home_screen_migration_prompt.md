# 防災アプリのホーム画面・認証導線 移植プロンプト

以下の要件を、現在のシステム構成・フレームワーク・命名規則に合わせて実装してください。
既存の避難所データ、気象情報取得、管理者機能、指示データなど、今回の対象外の機能は壊さずに維持してください。

## 目的

現在の防災アプリにある次の部分だけを、このシステムへ移植してください。

- ホーム画面のデザイン
- 避難所マップの初期表示
- ブラウザの現在地表示
- ログイン状態に応じたホーム画面のボタン表示
- ログイン、ログアウト、管理者ページへの画面遷移

## 1. ホーム画面のデザイン

ホーム画面は、住民がスマートフォンでも使いやすい、明るく見やすい防災アプリの画面にしてください。

### 共通レイアウト

- ページ全体の背景は淡い青系の背景色にする。
- 上部に青色のヘッダーを配置する。
- ヘッダー左側にアプリ名「防災アプリ」を表示する。
- ヘッダー右側に、`ホームへ戻る` という文字が書かれたボタンを表示する。
- `ホームへ戻る` ボタンを押すとホーム画面へ遷移する。
- コンテンツ幅は広すぎないように制限し、スマートフォンでは画面幅に合わせて縮小する。
- 各情報は白いパネルまたはカードとして区切り、角を少し丸め、適度な余白と薄い影を付ける。
- 高齢者にも読みやすいよう、文字色と背景色のコントラストを確保する。
- ボタンはタップしやすい大きさにする。

### ホーム画面の表示順

ホーム画面には、次の順番で表示してください。

1. ログイン状態に応じたホームメニュー
2. 避難所マップ
3. 気象警報・注意報
4. 住民向けの避難・防災のお知らせ

見出し名は次のとおりにしてください。

- 避難所マップ
- 青森市 気象警報・注意報
- 避難・防災のお知らせ

## 2. ホームメニューの表示条件

認証状態に応じて、ホーム画面に表示するボタンを切り替えてください。

### 未ログイン時

次の2つだけを表示してください。

- `ログイン`
  - 管理者ログイン画面へ遷移する。
- `避難所検索`
  - 避難所検索画面へ遷移する。
  - オレンジ色のボタンにする。

### 管理者ログイン時

次の4つを表示してください。

- `避難所登録`
  - 避難所登録画面へ遷移する。
- `指示ボード`
  - 管理者向けの指示ボードへ遷移する。
  - ボタン名は変更せず、必ず「指示ボード」と表示する。
- `避難所検索`
  - 避難所検索画面へ遷移する。
  - 未ログイン時と同じくオレンジ色にする。
- `ログアウト`
  - セッションを破棄し、ホーム画面へ遷移する。

管理者向けの `避難所登録` と `指示ボード` は、未ログイン時にはホーム画面に表示しないでください。
サーバー側でも管理者認証を必須にし、URLを直接入力されても未ログインでは利用できないようにしてください。

## 3. 避難所マップの初期表示

ホーム画面の避難所マップには、Leafletまたは対象システムで既に利用している地図ライブラリを使用してください。

- 地図の初期中心は青森市にする。
- 初期座標は緯度 `40.8244`、経度 `140.7400` を使用する。
- 初期ズームは `12` 程度にする。
- マップの高さはスマートフォンでも操作しやすい固定高さにする。
- OpenStreetMapなど、既存システムで利用可能な地図タイルを表示する。
- 地図提供元の利用規約に従い、必要な帰属表示を行う。
- マップ見出しのアイコンは丸印ではなく、位置を示すピンアイコンにする。
- 既存の避難所マーカーがある場合は、それを維持して地図上に表示する。

## 4. 現在地表示

マップ上に現在地表示ボタンを追加してください。

- ボタンは地図右上に配置する。
- 現在地を示すアイコンまたは分かりやすい記号を使用する。
- ボタンには `現在地を表示` のツールチップまたはアクセシブルなラベルを付ける。
- 押すとブラウザのGeolocation APIで現在地を取得する。
- 現在地取得後は、地図の中心を現在地へ移動する。
- 現在地を示すマーカーを地図上に表示する。
- 既に現在地マーカーがある場合は、新しいマーカーを増やさず位置を更新する。
- 現在地取得中はボタンを一時的に無効化し、処理中であることが分かるようにする。
- 成功時は `現在地を表示しました` と表示する。
- 位置情報が許可されなかった場合は `位置情報の利用が許可されていません` と表示する。
- 位置情報が取得できなかった場合は `現在地を取得できませんでした` と表示する。
- ブラウザがGeolocation APIに対応していない場合は、その旨を表示する。
- 位置情報の利用は、ボタンを押したときだけ開始し、ページ表示時に自動取得しない。
- HTTPSまたはlocalhostなど、ブラウザが位置情報を許可する実行環境を考慮する。

## 5. 認証と画面遷移

既存の認証方式がある場合はそれを利用し、認証の仕組みを重複して作らないでください。

必要な動作は次のとおりです。

- 管理者ログインが成功した場合、通常はホーム画面へ遷移する。
- ログイン画面へ遷移する前に、認証が必要なページへのアクセス先が保存されている場合は、ログイン成功後に元のページへ戻す。
- `next` などの戻り先URLを使う場合は、外部サイトへリダイレクトできないよう安全性を確認する。
- ログイン済みの管理者がログイン画面へアクセスした場合は、ホーム画面または指定された安全な戻り先へ遷移する。
- ログアウト時は認証セッションを破棄し、ホーム画面へ遷移する。
- 未ログイン状態で避難所登録または指示ボードへアクセスした場合は、ログイン画面へ遷移する。
- 未ログイン状態でも避難所検索とホーム画面は利用できる。

## 6. 実装時の制約

- 今回指定した機能以外の既存機能を削除・変更しない。
- 既存のルート、API、データ形式、管理者権限を可能な限り維持する。
- 既存のテンプレート構成やコンポーネント構成に合わせる。
- 外部ライブラリを追加する場合は、既存の依存関係を確認し、必要な設定も追加する。
- HTMLのアクセシビリティを確保する。ボタンやリンクには意味のあるラベルを付ける。
- PCとスマートフォンの両方でレイアウトが崩れないようにする。
- 絵文字を使う場合でも、意味を伝えるaria-labelや代替テキストを付ける。

## 7. 完了条件

実装後、次の項目をすべて確認してください。

- 未ログインでホーム画面を開くと、`ログイン` とオレンジ色の `避難所検索` が表示される。
- 未ログイン時に `避難所登録` と `指示ボード` がホーム画面に表示されない。
- 管理者でログインすると、ホーム画面へ遷移する。
- 管理者ログイン後も `避難所検索` が表示される。
- 管理者ログイン後に `避難所登録`、`指示ボード`、`ログアウト` が表示される。
- ログアウトするとホーム画面へ戻り、管理者向けボタンが消える。
- ホーム画面右上の `ホームへ戻る` を押すとホーム画面へ戻る。
- 避難所マップが青森市を中心に初期表示される。
- マップ右上の現在地ボタンを押すと現在地取得が始まる。
- 現在地取得成功時に地図が現在地へ移動し、現在地マーカーが表示される。
- 位置情報を拒否した場合や取得に失敗した場合に、日本語のエラーメッセージが表示される。
- 未ログイン状態で管理者ページのURLへ直接アクセスしても利用できない。
- 既存の避難所検索、気象情報、住民向けお知らせが壊れていない。
- テンプレート、JavaScript、サーバー側コードのエラーがない。
- 可能であれば、未ログイン時・ログイン時・ログアウト後・位置情報成功時・位置情報拒否時のテストを実行する。

最後に、変更したファイル、実装した機能、実行したテスト、未確認の点を簡潔に報告してください。
